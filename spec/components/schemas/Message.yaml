type: object
required:
  - messageId
  - text
  - userId
  - timestamp
properties:
  messageId:
    $ref: '#/components/schemas/MessageID'
  text:
    type: string
  userId:
    $ref: '#/components/schemas/UserID'
  timestamp:
    type: string
